# Nothing to Add - 技术选型文档

> **核心工具及其功能说明**
>
> 每个环节用什么工具、为什么用、实现什么功能

---

## 📋 目录

1. [数据收集与清洗](#1-数据收集与清洗)
2. [RAG系统](#2-rag系统)
3. [大语言模型集成](#3-大语言模型集成)
4. [后端API](#4-后端api)
5. [前端界面](#5-前端界面)
6. [部署上线](#6-部署上线)
7. [开发工具](#7-开发工具)

---

## 1. 数据收集与清洗

### 📥 数据收集

#### 工具：`requests` + `BeautifulSoup4` (Python)

**功能**：
- 下载PDF文件
- 抓取网页内容
- 访问公开API

**为什么选它**：
- ✅ Python最流行的HTTP库
- ✅ 简单易用
- ✅ 社区成熟

**安装**：
```bash
pip install requests beautifulsoup4
```

**使用场景**：
```python
import requests
from bs4 import BeautifulSoup

# 下载PDF
response = requests.get("https://example.com/letter.pdf")
with open("letter.pdf", "wb") as f:
    f.write(response.content)

# 抓取网页
response = requests.get("https://example.com")
soup = BeautifulSoup(response.content, 'html.parser')
text = soup.get_text()
```

---

### 📄 PDF提取

#### 工具：`pypdf` (Python)

**功能**：
- 从PDF中提取文字
- 支持加密PDF
- 处理多页PDF

**为什么选它**：
- ✅ 纯Python，无需额外依赖
- ✅ 性能好，速度快
- ✅ 支持中文

**安装**：
```bash
pip install pypdf
```

**使用场景**：
```python
from pypdf import PdfReader

reader = PdfReader("buffett_letter.pdf")

# 提取所有文字
for page in reader.pages:
    text = page.extract_text()
    print(text)
```

---

### 🧹 数据清洗

#### 工具：Python标准库 + `re` (正则表达式)

**功能**：
- 去除多余空格
- 统一格式
- 分割文本
- 清洗特殊字符

**为什么选它**：
- ✅ 内置功能，无需安装
- ✅ 灵活强大
- ✅ 性能好

**使用场景**：
```python
import re

def clean_text(text):
    # 去除多余空格
    text = " ".join(text.split())

    # 去除特殊字符
    text = re.sub(r'[^\w\s\u4e00-\u9fff.,!?;:]', '', text)

    # 统一换行符
    text = text.replace('\r\n', '\n')

    return text
```

---

### 📝 文本存储格式

#### 工具：Markdown (.md文件)

**功能**：
- 轻量级标记语言
- 易读易写
- 支持中文
- 版本控制友好

**为什么选它**：
- ✅ 人类可读
- ✅ Git友好
- ✅ 易于转换

**格式示例**：
```markdown
# 2024年巴菲特致股东信

## 第1页
亲爱的股东们：

...

## 第2页
...
```

---

## 2. RAG系统

### 🔍 文档切分

#### 工具：`LlamaIndex` (SentenceSplitter)

**功能**：
- 将长文档切分成小段
- 保持语义完整性
- 支持重叠切分

**为什么选它**：
- ✅ 专为RAG设计
- ✅ 智能切分
- ✅ 可配置参数

**安装**：
```bash
pip install llama-index
```

**配置**：
```python
from llama_index.node_parser import SentenceSplitter

splitter = SentenceSplitter(
    chunk_size=400,      # 每块大小（字符数）
    chunk_overlap=50,    # 重叠部分
    separator="\n\n"     # 分隔符
)
```

---

### 🎯 文本向量化

#### 工具：`SentenceTransformers` (BAAI/bge-small-en-v1.5)

**功能**：
- 将文本转换成向量
- 支持中英文
- 语义相似度计算

**为什么选它**：
- ✅ 中文友好
- ✅ 性能优秀
- ✅ 开源免费

**安装**：
```bash
pip install sentence-transformers
```

**使用**：
```python
from sentence_transformers import SentenceTransformer

model = SentenceTransformer('BAAI/bge-small-en-v1.5')
embeddings = model.encode(["什么是价值投资", "如何选股"])
```

---

### 💾 向量数据库

#### 工具：`ChromaDB`

**功能**：
- 存储和检索向量
- 支持元数据过滤
- 持久化存储
- 本地运行

**为什么选它**：
- ✅ 完全本地，无需云服务
- ✅ 开源免费
- ✅ 与LlamaIndex深度集成
- ✅ 易于使用

**安装**：
```bash
pip install chromadb
```

**使用**：
```python
import chromadb

# 创建客户端
client = chromadb.PersistentClient(path="./data/chroma")

# 创建集合
collection = client.get_or_create_collection("buffett_munger")

# 存储向量
collection.add(
    embeddings=[[1.0, 2.0, 3.0]],
    documents=["什么是价值投资"],
    metadatas=[{"source": "2024_letter"}]
)
```

---

### 🔎 语义检索

#### 工具：`LlamaIndex` (QueryEngine)

**功能**：
- 基于查询检索相关文档
- 相似度排序
- 支持多种检索策略

**为什么选它**：
- ✅ RAG一站式解决方案
- ✅ API简洁
- ✅ 可扩展性强

**使用**：
```python
from llama_index import VectorStoreIndex

index = VectorStoreIndex.from_documents(documents)

query_engine = index.as_query_engine(
    similarity_top_k=3,    # 返回最相关的3个结果
    response_mode="compact"  # 紧凑模式
)

response = query_engine.query("什么是价值投资？")
```

---

## 3. 大语言模型集成

### 🤖 主力模型：GLM-4.7（智谱AI）

**功能**：
- 生成高质量回答
- 理解复杂问题
- 中文优化

**为什么选它**：
- ✅ 用户已订阅（零边际成本）
- ✅ 中文能力强
- ✅ 性能接近GPT-4
- ✅ 国内访问稳定

**安装**：
```bash
pip install zhipuai
```

**使用**：
```python
from zhipuai import ZhipuAI

client = ZhipuAI(api_key=os.getenv("ZHIPU_API_KEY"))

response = client.chat.completions.create(
    model="glm-4.7",
    messages=[{"role": "user", "content": "问题"}]
)

answer = response.choices[0].message.content
```

---

### 🆓 备用模型：HuggingFace免费API (Llama-3.3-70B)

**功能**：
- 处理简单问题
- 降低成本
- 高可用性

**为什么选它**：
- ✅ 完全免费
- ✅ 性能不错
- ✅ 无需API key
- ✅ 作为GLM-4.7的备份

**安装**：
```bash
pip install huggingface-hub
```

**使用**：
```python
from huggingface_hub import InferenceClient

client = InferenceClient(model="meta-llama/Llama-3.3-70B-Instruct")

response = client.text_generation(
    prompt="问题",
    max_new_tokens=500
)
```

---

### 🧠 智能路由

#### 工具：自定义路由逻辑

**功能**：
- 根据问题复杂度选择模型
- 自动降级
- 负载均衡

**实现**：
```python
def classify_task(question: str) -> str:
    """判断任务复杂度"""
    simple_keywords = ["你好", "谢谢", "简单"]
    complex_keywords = ["分析", "为什么", "如何"]

    if any(kw in question for kw in simple_keywords):
        return "simple"  # 用免费API
    else:
        return "complex"  # 用GLM-4.7
```

---

## 4. 后端API

### 🌐 Web框架：FastAPI

**功能**：
- 构建RESTful API
- 自动生成API文档
- 异步支持
- 数据验证

**为什么选它**：
- ✅ 现代化的Python Web框架
- ✅ 性能优秀（接近NodeJS）
- ✅ 自动生成OpenAPI文档
- ✅ 类型安全（Pydantic）
- ✅ 异步支持

**安装**：
```bash
pip install fastapi uvicorn[standard]
```

**核心功能**：
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="Nothing to Add API")

# CORS支持（允许前端调用）
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.post("/api/chat")
async def chat(request: ChatRequest):
    """对话接口"""
    return {"response": "答案"}
```

---

### 📊 数据验证：Pydantic

**功能**：
- 请求数据验证
- 自动类型转换
- 错误提示

**为什么选它**：
- ✅ 与FastAPI深度集成
- ✅ 减少验证代码
- ✅ 类型安全

**使用**：
```python
from pydantic import BaseModel, Field

class ChatRequest(BaseModel):
    message: str = Field(..., min_length=1)
    mode: str = Field(default="buffett")
```

---

### ⚡ 异步处理

**功能**：
- 并发处理请求
- 提高性能
- 非阻塞IO

**实现**：
```python
@app.post("/api/chat")
async def chat(request: ChatRequest):
    # 异步调用LLM
    response = await llm_service.generate_async(request.message)
    return response
```

---

## 5. 前端界面

### ⚛️ 框架：React 18 + TypeScript

**功能**：
- 构建用户界面
- 组件化开发
- 类型安全

**为什么选它**：
- ✅ 最流行的前端框架
- ✅ 生态成熟
- ✅ 就业市场需求大
- ✅ TypeScript提供类型安全

**安装**：
```bash
npm create vite@latest frontend -- --template react-ts
```

---

### 🎨 样式：TailwindCSS

**功能**：
- 快速构建样式
- 响应式设计
- 深色模式支持

**为什么选它**：
- ✅ 不用写CSS文件
- ✅ 原子类，易于维护
- ✅ 设计一致性好
- ✅ 性能优秀（自动清除未使用样式）

**安装**：
```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

**使用**：
```tsx
<div className="flex items-center justify-between p-4 bg-white rounded-lg shadow">
  内容
</div>
```

---

### 🔄 状态管理：React Hooks

**功能**：
- 管理组件状态
- 副作用处理
- 性能优化

**为什么选它**：
- ✅ React内置
- ✅ 简单直观
- ✅ 无需额外库

**核心Hooks**：
```tsx
import { useState, useEffect } from 'react';

function ChatBox() {
  const [messages, setMessages] = useState([]);
  const [isLoading, setIsLoading] = useState(false);

  useEffect(() => {
    // 副作用
  }, []);
}
```

---

### 📡 HTTP客户端：Axios

**功能**：
- 发送HTTP请求
- 拦截器
- 错误处理

**为什么选它**：
- ✅ API简洁
- ✅ 自动JSON转换
- ✅ 请求/响应拦截
- ✅ 浏览器兼容性好

**安装**：
```bash
npm install axios
```

**使用**：
```tsx
import axios from 'axios';

const response = await axios.post('/api/chat', {
  message: "问题",
  mode: "buffett"
});
```

---

### 🎯 UI组件库：Lucide React

**功能**：
- 提供精美图标
- 树摇优化
- TypeScript支持

**为什么选它**：
- ✅ 轻量级
- ✅ 风格统一
- ✅ 易于使用

**安装**：
```bash
npm install lucide-react
```

**使用**：
```tsx
import { Send, MessageSquare } from 'lucide-react';

<Send className="w-5 h-5" />
```

---

## 6. 部署上线

### 🚀 前端部署：Vercel

**功能**：
- 自动部署React应用
- CDN加速
- HTTPS证书
- 预览环境

**为什么选它**：
- ✅ 零配置部署
- ✅ 全球CDN
- ✅ 免费额度慷慨
- ✅ GitHub集成
- ✅ 自动HTTPS

**部署步骤**：
```bash
# 1. 安装Vercel CLI
npm i -g vercel

# 2. 登录
vercel login

# 3. 部署
vercel

# 4. 配置环境变量
# 在Vercel Dashboard配置
```

**配置文件**：
```json
// vercel.json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "env": {
    "VITE_API_URL": "https://api.example.com"
  }
}
```

---

### 🚂 后端部署：Railway

**功能**：
- 部署Python应用
- 自动扩展
- 环境变量管理
- 日志查看

**为什么选它**：
- ✅ 支持Python/FastAPI
- ✅ 免费额度充足
- ✅ 简单易用
- ✅ 自动部署（从GitHub）

**部署步骤**：
```bash
# 1. 推送代码到GitHub
git push

# 2. 在Railway新建项目
# 3. 连接GitHub仓库
# 4. 配置环境变量
# 5. 自动部署
```

**配置文件**：
```bash
# Procfile
web: uvicorn app.main:app --host 0.0.0.0 --port $PORT

# runtime.txt
python-3.11.0
```

---

### 🔧 环境变量管理

**工具**：`.env`文件 + `python-dotenv`

**功能**：
- 存储敏感信息
- 不同环境配置
- 不提交到Git

**为什么选它**：
- ✅ 安全（不暴露密钥）
- ✅ 灵活（多环境）
- ✅ 标准做法

**使用**：
```bash
# .env（不提交到Git）
ZHIPU_API_KEY=your_key_here
HF_TOKEN=your_token_here

# .env.example（提交到Git）
ZHIPU_API_KEY=your_zhipu_api_key
HF_TOKEN=your_huggingface_token
```

---

## 7. 开发工具

### 📝 代码编辑器：VS Code

**推荐插件**：
- Python（Microsoft）
- Pylance（Microsoft）
- ES7+ React/Redux/React-Native snippets（dsznajder）
- Tailwind CSS IntelliSense（tailwindlabs）
- GitLens（GitKraken）

---

### 🐛 调试工具

**前端**：
- Chrome DevTools（浏览器内置）
- React DevTools（浏览器插件）

**后端**：
- FastAPI自动生成的Swagger UI (`/docs`)
- Python debugger (`pdb`)

---

### 🧪 测试工具

**后端测试**：
```bash
# pytest
pip install pytest pytest-asyncio

# 运行测试
pytest tests/
```

**API测试**：
- FastAPI Swagger UI (`http://localhost:8000/docs`)
- Postman
- curl命令

---

### 📊 版本控制：Git + GitHub

**功能**：
- 代码版本管理
- 分支协作
- 代码备份

**为什么选它**：
- ✅ 行业标准
- ✅ GitHub免费私有仓库
- ✅ 便于协作

**基础命令**：
```bash
git add .
git commit -m "描述"
git push
```

---

## 📊 技术栈总览

### 开发阶段

```
数据层：
├─ requests: 数据下载
├─ pypdf: PDF提取
├─ Python内置: 数据清洗
└─ Markdown: 数据存储

RAG层：
├─ LlamaIndex: RAG框架
├─ SentenceTransformers: 向量化
└─ ChromaDB: 向量数据库

AI层：
├─ zhipuai: GLM-4.7（主力）
└─ huggingface-hub: Llama-3.3-70B（备用）

后端：
├─ FastAPI: Web框架
├─ Pydantic: 数据验证
└─ Uvicorn: ASGI服务器

前端：
├─ React 18: UI框架
├─ TypeScript: 类型安全
├─ TailwindCSS: 样式
├─ Axios: HTTP客户端
└─ Lucide React: 图标
```

### 部署阶段

```
前端：
└─ Vercel: 自动部署

后端：
└─ Railway: Python应用部署

版本控制：
└─ Git + GitHub: 代码管理
```

---

## 💰 成本总结

### 开发阶段
```
所有工具：$0（全部使用免费/开源工具）
```

### 部署阶段
```
前端（Vercel）：$0（免费额度足够）
后端（Railway）：$0（免费额度足够）
GLM-4.7：$0（已订阅）
HuggingFace：$0（免费API）
总计：$0/月 ✅
```

---

## 📚 学习资源

### 官方文档
- [FastAPI](https://fastapi.tiangolo.com/)
- [React](https://react.dev/)
- [LlamaIndex](https://docs.llamaindex.ai/)
- [TailwindCSS](https://tailwindcss.com/)
- [Vercel](https://vercel.com/docs)
- [Railway](https://docs.railway.app/)

---

**文档版本**：v1.0
**最后更新**：2026-01-17
**维护者**：Nothing to Add 项目团队
